{block title}Kraje{/block}
{block content}
  <h1 class="h3 mb-3">Kraje</h1>

  <div class="list-group shadow-sm">
    {foreach $kraje as $k}
      <a class="list-group-item list-group-item-action d-flex justify-content-between align-items-center"
         n:href="Okres:default, krajId => $k->ID">
        <span>{$k->Kraj}</span>
        <span class="badge bg-secondary rounded-pill">ID {$k->ID}</span>
      </a>
    {/foreach}
  </div>
{/block}
